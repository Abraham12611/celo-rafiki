import Logger from 'bunyan';
import { Response } from 'express';
import { OdisRequest, OdisResponse } from '..';
export declare function send<I extends OdisRequest = OdisRequest, O extends OdisResponse<I> = OdisResponse<I>>(response: Response<O>, body: O, status: number, logger: Logger): void;

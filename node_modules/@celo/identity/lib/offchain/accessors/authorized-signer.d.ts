import { Address } from '@celo/base';
import { OffchainDataWrapper, OffchainErrors } from '../../offchain-data-wrapper';
export declare class AuthorizedSignerAccessor {
    readonly wrapper: OffchainDataWrapper;
    basePath: string;
    constructor(wrapper: OffchainDataWrapper);
    readAsResult(account: Address, signer: Address): Promise<import("@celo/base").Result<{
        address: string;
        proofOfPossession: string;
        filteredDataPaths: string;
    }, import("./errors").SchemaErrors>>;
    read: (account: string, signer: string) => Promise<{
        address: string;
        proofOfPossession: string;
        filteredDataPaths: string;
    }>;
    write(signer: Address, proofOfPossession: string, filteredDataPaths: string): Promise<OffchainErrors | void>;
}

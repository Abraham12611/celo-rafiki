/// <reference types="node" />
export declare abstract class StorageWriter {
    abstract write(_data: Buffer, _dataPath: string): Promise<void>;
}
export declare class LocalStorageWriter extends StorageWriter {
    readonly root: string;
    constructor(root: string);
    write(data: Buffer, dataPath: string): Promise<void>;
    protected writeToFs(data: string | Buffer, dataPath: string): Promise<void>;
}
export declare class GitStorageWriter extends LocalStorageWriter {
    write(data: Buffer, dataPath: string): Promise<void>;
}
export declare class GoogleStorageWriter extends LocalStorageWriter {
    readonly local: string;
    private readonly bucket;
    constructor(local: string, bucket: string);
    write(data: Buffer, dataPath: string): Promise<void>;
}
export declare class AwsStorageWriter extends LocalStorageWriter {
    readonly local: string;
    private readonly bucket;
    constructor(local: string, bucket: string);
    write(data: Buffer, dataPath: string): Promise<void>;
}
export declare class MockStorageWriter extends LocalStorageWriter {
    readonly root: string;
    readonly mockedStorageRoot: string;
    readonly fetchMock: any;
    constructor(root: string, mockedStorageRoot: string, fetchMock: any);
    write(data: Buffer, dataPath: string): Promise<void>;
}
